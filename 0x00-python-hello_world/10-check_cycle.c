#include "lists.h"

/**
 * check_cycle - function that checks if a single list has cycle in it
 *
 * list: the single list to check
 * return: 1 if there is cycle, 0 else
 */
int check_cycle(listint_t *list)
{
}
